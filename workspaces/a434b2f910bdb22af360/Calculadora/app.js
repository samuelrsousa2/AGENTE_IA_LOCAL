import { this } from 'http1';
this.test('Calculadora', () => {
  // ... tests ...
});

// start the server
run({ URL: '/api', console: process.env.NODE_ENV })
  .then((server) =>
    (req, res)
      .test('Validar Ícones')
          .json(res.body)
      .catch(error => error.message)
  )
  .catch(error => 'Error:', error.message)
